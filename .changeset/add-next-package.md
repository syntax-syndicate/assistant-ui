---
"@assistant-ui/next": patch
---

feat: add @assistant-ui/next — the `withAui()` Next.js config wrapper, the `defineToolkit()` authoring helper, and the compiler for the `"use generative"` directive that colocates a tool's schema, server-only `execute`, and client-only `render` in one file
