---
"@assistant-ui/x-generative-compiler": patch
---

feat: pass `unstable_interactableTool(...)` toolkit entries through `"use generative"` compilation — the client build keeps the config's `render`, the server build drops it
